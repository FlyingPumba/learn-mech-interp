---
phase: 05-navigation-information-architecture
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/index.njk
  - src/css/components.css
autonomous: true

must_haves:
  truths:
    - "The homepage hero section links to the first article in the learning path (attention-mechanism), not the test article"
    - "The homepage displays a visual learning path showing all blocks with their topics in suggested reading order"
    - "Each topic in the learning path links to its article page"
    - "The homepage has a clear 'start here' entry point directing users to the first article"
  artifacts:
    - path: "src/index.njk"
      provides: "Homepage with hero section, start here link, and learning path visualization"
      contains: "learningPath"
    - path: "src/css/components.css"
      provides: "Styles for learning path visualization on homepage"
      contains: "learning-path"
  key_links:
    - from: "src/index.njk"
      to: "src/_data/learningPath.json"
      via: "learningPath global data iterated to render blocks and topics"
      pattern: "learningPath.blocks"
---

<objective>
Update the homepage with a working "start here" link pointing to the first real article, and add a learning path visualization showing the suggested reading order through all blocks and topics.

Purpose: New visitors see the full learning path and know where to start. The homepage becomes a meaningful entry point rather than a placeholder.
Output: Homepage with corrected hero link and learning path visualization using learningPath.json data.
</objective>

<execution_context>
@/Users/ivan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navigation-information-architecture/05-RESEARCH.md
@.planning/phases/05-navigation-information-architecture/05-01-SUMMARY.md
@src/index.njk
@src/css/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage with learning path visualization and corrected hero link</name>
  <files>src/index.njk</files>
  <action>
    Rewrite `src/index.njk` to fix the hero "Start Learning" link (currently points to `/topics/test/` which is the test article) and add a learning path section. Keep the existing hero section structure and feature cards, but fix the link and add the learning path between them:

    ```nunjucks
    ---
    title: Home
    layout: layouts/base.njk
    ---
    <section class="hero">
      <div class="hero-content">
        <h1 class="hero-title">Learn Mechanistic Interpretability</h1>
        <p class="hero-description">Structured articles covering transformer internals, interpretability techniques, and frontier research.</p>
        <div class="hero-actions">
          {% set firstTopic = learningPath.blocks[0].topics[0] %}
          <a href="/topics/{{ firstTopic.slug }}/" class="btn btn-primary">Start Learning</a>
          <a href="/topics/" class="btn btn-secondary">Browse Topics</a>
        </div>
      </div>
    </section>

    <section class="learning-path">
      <div class="learning-path-content">
        <h2 class="learning-path-title">Learning Path</h2>
        <p class="learning-path-description">Follow this suggested reading order from foundations to advanced topics, or jump to any article.</p>

        <ol class="learning-path-blocks">
          {% for block in learningPath.blocks %}
          <li class="learning-path-block">
            <div class="learning-path-block-header">
              <span class="learning-path-block-number">{{ loop.index }}</span>
              <h3 class="learning-path-block-title">{{ block.title }}</h3>
            </div>
            <ol class="learning-path-topics">
              {% for topic in block.topics %}
              <li class="learning-path-topic">
                <a href="/topics/{{ topic.slug }}/">{{ topic.title }}</a>
              </li>
              {% endfor %}
            </ol>
          </li>
          {% endfor %}
        </ol>
      </div>
    </section>

    <section class="features">
      <h2 class="visually-hidden">Features</h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">📚</div>
          <h3>Structured Learning</h3>
          <p>Articles organized by topic and difficulty, from foundations to advanced research.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">🔬</div>
          <h3>Research-Based</h3>
          <p>Content drawn from key papers with direct links to original sources.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">💡</div>
          <h3>Build Intuition</h3>
          <p>Focus on understanding concepts deeply, not just memorizing definitions.</p>
        </div>
      </div>
    </section>
    ```

    Key changes from existing index.njk:
    1. Hero "Start Learning" button now links to the first topic from learningPath.json (attention-mechanism) instead of `/topics/test/`
    2. New "Learning Path" section between hero and features, showing numbered blocks with linked topics
    3. Feature cards section is preserved unchanged
    4. The learning path uses `learningPath` global data (available automatically from `_data/learningPath.json`)
  </action>
  <verify>
    - `npx @11ty/eleventy` builds without errors
    - Built `_site/index.html` contains href="/topics/attention-mechanism/" in the hero section
    - Built `_site/index.html` contains "Learning Path" heading
    - Built `_site/index.html` contains links to all 3 pilot articles
    - Built `_site/index.html` does NOT contain href="/topics/test/"
  </verify>
  <done>Homepage hero links to first real article. Learning path section shows 3 blocks with their topics as clickable links. Feature cards preserved.</done>
</task>

<task type="auto">
  <name>Task 2: CSS for learning path visualization</name>
  <files>src/css/components.css</files>
  <action>
    Add learning path styles to `src/css/components.css` at the end of the file:

    ```css
    /* ==========================================================================
       LEARNING PATH
       Visual learning path on homepage showing suggested reading order.
       Numbered blocks with linked topics.
       ========================================================================== */

    .learning-path {
      padding: var(--spacing-2xl, 3rem) var(--container-padding, 1.5rem);
      border-bottom: 1px solid var(--color-border, rgba(0, 0, 0, 0.06));
    }

    .learning-path-content {
      max-width: 680px;
      margin: 0 auto;
    }

    .learning-path-title {
      font-size: var(--font-size-h2, 1.5rem);
      font-weight: 600;
      margin: 0 0 var(--spacing-sm, 0.5rem) 0;
      text-align: center;
    }

    .learning-path-description {
      text-align: center;
      color: var(--color-text-secondary, rgba(0, 0, 0, 0.6));
      margin: 0 0 var(--spacing-xl, 2rem) 0;
      font-size: 1.0625rem;
      line-height: 1.6;
    }

    .learning-path-blocks {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .learning-path-block {
      margin-bottom: var(--spacing-lg, 1.5rem);
      padding: var(--spacing-lg, 1.5rem);
      border: 1px solid var(--color-border, rgba(0, 0, 0, 0.08));
      border-radius: var(--radius-lg, 8px);
      transition: border-color var(--transition-base, 200ms ease);
    }

    .learning-path-block:hover {
      border-color: var(--color-border-strong, rgba(0, 0, 0, 0.15));
    }

    .learning-path-block-header {
      display: flex;
      align-items: center;
      gap: var(--spacing-md, 1rem);
      margin-bottom: var(--spacing-md, 1rem);
    }

    .learning-path-block-number {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 2rem;
      height: 2rem;
      border-radius: 50%;
      background: var(--color-link, #004276);
      color: #ffffff;
      font-size: var(--font-size-small, 0.875rem);
      font-weight: 600;
      flex-shrink: 0;
    }

    .learning-path-block-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin: 0;
      color: var(--color-text, rgba(0, 0, 0, 0.87));
    }

    .learning-path-topics {
      list-style: none;
      margin: 0;
      padding: 0 0 0 calc(2rem + var(--spacing-md, 1rem));
    }

    .learning-path-topic {
      margin-bottom: 0.25rem;
    }

    .learning-path-topic a {
      display: inline-block;
      padding: 0.25rem 0;
      color: var(--color-link, #004276);
      text-decoration: none;
      font-size: 0.9375rem;
    }

    .learning-path-topic a:hover {
      text-decoration: underline;
    }

    /* Connecting line between blocks */
    .learning-path-block + .learning-path-block {
      position: relative;
    }

    .learning-path-block + .learning-path-block::before {
      content: "";
      display: block;
      width: 2px;
      height: var(--spacing-lg, 1.5rem);
      background: var(--color-border, rgba(0, 0, 0, 0.1));
      margin: calc(-1 * var(--spacing-lg, 1.5rem)) auto var(--spacing-lg, 1.5rem);
    }

    @media (min-width: 768px) {
      .learning-path {
        padding: var(--spacing-3xl, 4rem) var(--spacing-xl, 2rem);
      }
    }
    ```

    The learning path uses a numbered-block card pattern that mirrors the feature cards aesthetic. Each block has a numbered circle, a title, and indented topic links. A subtle connecting line between blocks suggests progression.
  </action>
  <verify>
    - `npx @11ty/eleventy` builds without errors
    - Built CSS contains `.learning-path-block`, `.learning-path-topic`, `.learning-path-block-number` classes
    - The overall visual style is consistent with existing feature cards (same border radius, spacing tokens, transition patterns)
  </verify>
  <done>Learning path visualization styled with numbered block cards, linked topics, connecting lines, and responsive adjustments. Consistent with existing design tokens and component patterns.</done>
</task>

</tasks>

<verification>
1. `npx @11ty/eleventy` builds without errors
2. Homepage hero "Start Learning" links to `/topics/attention-mechanism/` (not `/topics/test/`)
3. Homepage shows "Learning Path" section with 3 numbered blocks
4. Each block shows its topics as clickable links to article pages
5. All 3 article links work: attention-mechanism, superposition, activation-patching
6. Feature cards section is preserved after the learning path
7. Learning path CSS uses existing design tokens (--color-link, --radius-lg, --spacing-*, etc.)
8. Page renders well on narrow viewports (no horizontal overflow)
</verification>

<success_criteria>
- Homepage directs new users to the first real article, not the test article
- Learning path visualization shows all blocks and topics from learningPath.json
- Each topic in the learning path links to its article
- Visual design is consistent with existing feature cards and hero section
- Layout works on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/05-navigation-information-architecture/05-04-SUMMARY.md`
</output>
